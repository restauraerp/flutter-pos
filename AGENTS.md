# Resource Locations
* API documentation is at `../../core-api/api.json`
* API project is at `../../core-api/`
* Already developed nextjs frontend project `../../front`
* You will find project specific `AGENTS` file in their respective folder
* You can find the assets at `../../Assets` folder


# Project Notes
* This will be a flutter project
* This project is only for the pos terminal used by pos manager or higher authority users.
* Any user with pos permission can log in and use this application.
* Server needs to be set up before any user can log in
* After log out the user can manage the server URL on the log in page.

# Projects Builds
* Android
* iOS
* Windows Desktop
* Linux Desktop
* Mac Desktop
